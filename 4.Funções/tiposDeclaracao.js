// function declaration
function soma(x, y) {
    return x + y;
}

// Declarando uma função anônima a uma variável ou constante
// function expression

const sub = function (x, y) {
    return x - y;
}

// named fuction expression
const mult = function mult(x, y) {
    return x * y;
}